/** \file lcddemo.c
 *  \brief A simple demo that draws a string and square
 */

#include <libTimer.h>
#include "lcdutils.h"
#include "lcddraw.h"

void drawBox(u_char acol, u_char arrow, uchar size, u_int color){
  u_char max_col = acol + size;
  u_char max_row = arow +size;
  for(uchar col = acol -size; col < max_col; col
/** Initializes everything, clears the screen, draws "hello" and a square */
int
main()
{
  configureClocks();
  lcd_init();
  u_char width = screenWidth, height = screenHeight;

  clearScreen(COLOR_BLUE);

  // drawString5x7(20,20, "puto el que lo lea", COLOR_GREEN, COLOR_RED);

  //fillRectangle(30,30, 60, 60, COLOR_ORANGE);
  for(short i = 0; i < 20; i++){
    drawPixel(i,i, COLOR_PINK);
  }  
}
